# Flutter Widget Preview 使用指南

## 🎯 功能概述

Flutter Widget Preview 让你可以在不运行完整应用的情况下，在Chrome浏览器中实时预览单个Widget组件。

## 📋 系统要求

- ✅ Flutter 3.35+ (当前版本: 3.38.5)
- ✅ Chrome 浏览器
- ✅ 支持的IDE: VS Code, Android Studio, IntelliJ

## 🚀 启动方法

### 方法1: 命令行启动
```bash
flutter widget-preview start
```

### 方法2: IDE集成
- **VS Code**: 打开侧边栏中的"Flutter Widget Preview"面板
- **Android Studio/IntelliJ**: 打开侧边栏中的"Flutter Widget Preview"选项卡

## 📁 预览组件

当前项目包含以下预览组件：

### UI组件 (`simple_previews.dart`)
- 主要按钮
- 优先级徽章
- 时间选择按钮  
- 浮动操作按钮

### 任务组件 (`task_card_preview.dart`)
- 工作任务卡片
- 个人任务卡片
- 紧急任务卡片

### 日历组件 (`calendar_preview.dart`)
- 日历日期格子
- 日历导航按钮

### 对话框组件 (`dialog_preview.dart`)
- 任务创建对话框
- 时间选择器
- 优先级选择器

## 🔧 创建新预览

1. 在任意`.dart`文件中添加`@Preview`注解：

```dart
import 'package:flutter/widget_previews.dart';
import 'package:flutter/material.dart';

@Preview(
  name: '我的组件',
  group: '自定义组件',
  size: Size(300, 200),
)
Widget myComponentPreview() {
  return MaterialApp(
    home: Scaffold(
      body: Center(
        child: Text('Hello Preview!'),
      ),
    ),
  );
}
```

2. 保存文件，Widget Preview会自动检测新的预览

## 🎨 预览控制

每个预览提供以下控制选项：
- 🔍 放大/缩小
- 🔄 重置缩放
- 🌓 切换亮色/暗色主题
- 🔥 热重启单个预览

## 📝 注意事项

1. **依赖限制**: 不支持`dart:io`和`dart:ffi`相关API
2. **资源路径**: 使用基于包的路径，如`packages/my_package/assets/image.png`
3. **回调函数**: 所有回调参数必须是公共的且为常量
4. **未约束组件**: 会自动限制为预览器高度和宽度的一半

## 🐛 故障排除

如果预览无法启动：

1. 检查Flutter版本：`flutter --version`
2. 清理项目：`flutter clean && flutter pub get`
3. 检查依赖错误：确保所有导入的文件没有编译错误
4. 重新启动：`flutter widget-preview start`

## 💡 最佳实践

1. **组件隔离**: 每个预览应该是独立的，不依赖外部状态
2. **合理分组**: 使用`group`参数将相关预览组织在一起
3. **适当尺寸**: 设置合适的`size`参数避免布局问题
4. **主题一致**: 使用统一的主题配置保持视觉一致性

## 🔗 相关链接

- [Flutter Widget Preview 官方文档](https://docs.flutter.dev/tools/widget-preview)
- [Preview API 参考](https://api.flutter.dev/flutter/widget_previews/Preview-class.html)